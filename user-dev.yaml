app-info :
  name: User Management API
  description: CRUD operations for user management dev environment
  version: 1.0.0
  developer:
    name: Your Name
    password : "{cipher}eff43a565b203a1aaba7ad56744a27ec610ea14b219baef16d87a64e737cb9f5"
    email: your.email@example.com
  contact:
    name: Support Team
    email: support@test.com

eureka:
    instance:
        hostname: localhost
    client:
        fetch-registry: true
        register-with-eureka: true
        serviceUrl:
            defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
